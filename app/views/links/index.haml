.container
  .col-md-6.col-md-offset-3
    .main-card
      %h2.text-center Compress your links!
      %h4.text-center A simple URL shortener.
      = simple_form_for @link, remote: true, html: { class: 'text-center' } do |f|
        = f.input :original_url, as: :string, placeholder: t('forms.link.placeholders.original_url'), label: false
        = f.input :custom_url,   as: :string, placeholder: t('forms.link.placeholders.custom_url'), label: false
        = f.submit 'Compress!', class: 'btn btn-danger'